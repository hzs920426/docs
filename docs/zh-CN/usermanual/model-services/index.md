# 概览

::: info 文档信息
版本：v1.0
更新日期：2026-07-08
:::

## 功能概述

模型及AI服务面向模型发布、审核、体验、调用、日志和收益分析，帮助模型提供方发布模型，帮助模型调用方发现和使用模型，帮助运营管理员治理模型能力和审核流程。

#### 新手理解

模型及AI服务包含面向客户的模型服务页面和运营后台：模型提供方发布模型，模型调用方在 **"模型"** 和 **"体验中心"** 试用，运营管理员维护元模型、来源、模板和审核规则。

### 核心术语速查

| 术语 | 说明 |
| --- | --- |
| 元模型 | 描述模型协议、模态、Token 限制和默认参数的能力抽象。 |
| 模型来源 | 实际调用模型的 Endpoint、认证方式和请求头配置。 |
| 我的部署 | 查看云上快速部署后的模型部署记录，并从 `On-Cloud` 列表进入发布模型流程。 |
| 供应方 | 提供模型能力、价格和服务质量的主体。 |
| Personal Key | 用户用于调用模型服务的个人访问凭据。 |
| 审核单 | 模型或应用发布前的审核记录。 |
| 体验中心 | 用于文本、图像、音频或视频体验模型效果的页面。 |
| 调用日志 | 记录请求 ID、错误码、延迟、Token 和脱敏摘要的排障入口。 |
| 收益统计 | 按模型、客户和时间统计调用收益。 |

## 前提条件

1. 已确认当前账号是模型提供方、调用方还是运营管理员。
2. 发布模型前已准备模型来源、协议、价格、限流和安全说明。
3. 调用模型前已确认 Personal Key、Endpoint、额度和模型可见范围。
4. 查看收益或客户调用前，已确认当前账号具备对应数据权限。

## 页面说明

### 按角色阅读

| 角色 | 推荐入口 | 典型任务 |
| --- | --- | --- |
| 模型提供方 | [我的模型](./user/studio/my-models/)、[我的部署](./user/studio/my-deployments/)、[模型收益](./user/usage-earnings/model-earnings/) | 发布模型、维护版本、查看调用和收益；云上快速部署后的模型可进入 `创作空间 > 我的部署` 查看并继续发布。 |
| 模型调用方 | [模型](./user/discover/models/)、[体验中心](./user/playground/text/)、[我的调用](./user/my-calls/overview/) | 发现模型、体验效果、接入 API 并排查调用。 |
| 运营管理员 | [元模型](./operator/settings/meta-models/)、[模型来源](./operator/settings/model-source/)、[模型审核](./operator/approvals/model-reviews/) | 维护基础配置、审核模型和治理可见范围。 |

## 主要操作

### 选择角色入口

| 你的身份或目标 | 先看哪个入口 | 下一步 |
| --- | --- | --- |
| 新用户 | [模型及AI服务快速入门](./getting-started/) | 先理解元模型、模型来源、模板、发布、审核、市场和调用的关系。 |
| 完整流程 | [从发布到调用模型](./end-to-end/publish-and-call-model/) | 按端到端链路核对发布、审核、体验、调用、收益和排障入口。 |
| 运营管理员 | [元模型](./operator/settings/meta-models/)、[模型来源](./operator/settings/model-source/)、[模型审核](./operator/approvals/model-reviews/) | 维护基础配置、审核模型和治理可见范围。 |
| 模型提供方 | [我的模型](./user/studio/my-models/)、[我的部署](./user/studio/my-deployments/)、[模型收益](./user/usage-earnings/model-earnings/) | 发布模型、跟踪部署记录、查看收益和客户调用。 |
| 模型调用方 | [模型](./user/discover/models/)、[体验中心](./user/playground/text/)、[我的调用](./user/my-calls/overview/) | 浏览模型、体验效果、接入 API 并查看调用日志。 |

### 按目标定位页面

| 目标 | 推荐入口 | 下一步 |
| --- | --- | --- |
| 第一次了解模型服务 | [平台入门](./getting-started/) | 判断自己是模型提供方、调用方还是运营管理员。 |
| 查找并体验模型 | [模型](./user/discover/models/) | 进入体验中心后验证输入、输出和调用方式。 |
| 发布或维护模型 | [我的模型](./user/studio/my-models/) | 准备模型来源、部署配置、审核材料和 API 信息。 |
| 排查调用或收益 | [调用记录](./user/my-calls/overview/) | 结合调用日志、统计分析和收益数据交叉确认。 |

### 按推荐路径阅读

1. 新手先读 [模型及AI服务快速入门](./getting-started/) 选择角色，并完成一个页面可见的结果。
2. 运营管理员先维护元模型、来源、模板和标签。
3. 模型提供方在 [我的模型](./user/studio/my-models/) 发布模型；如先通过 On-Cloud 快速部署提交模型，可在 [我的部署](./user/studio/my-deployments/) 的 `On-Cloud` 列表查看部署记录并继续发布。
4. 模型调用方从 **"模型"** 进入 **"体验中心"**，再根据调用日志和用量数据排查问题。
5. 需要完整流程时阅读 [从发布到调用模型](./end-to-end/publish-and-call-model/)。

## 参数速查表

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 角色类型 | 是 | 枚举 | 模型提供方 | 用于判断先进入用户侧还是运营侧功能页。 |
| 模型对象 | 否 | 文本 | 示例模型 A | 用于定位 **"模型"**、我的模型、审核或调用记录中的目标模型。 |
| 调用凭据 | 否 | 文本 | `<PERSONAL_KEY>` | 用于确认调用方是否具备访问模型的有效凭据，文档中只写占位符。 |
| 审核状态 | 否 | 枚举 | 待审核 | 用于判断模型或应用是否可以继续上架和调用。 |
| 统计范围 | 否 | 时间范围 | 2026-07-01 至 2026-07-31 | 用于查看调用、收益和客户调用数据。 |

## 踩坑提示

- 不要把元模型、模型来源和已发布模型混为一谈；元模型定义能力，模型来源定义接入，已发布模型是面向模型调用方展示的记录。
- 体验中心调通不等于生产接入完成，正式调用前还要核对 Personal Key、限流、计费和错误日志。
- **"模型"** 页面不可见时不要只刷新页面，应同时检查审核状态、可见范围、标签和发布记录。

## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 角色入口 | 选中的链接打开与目标角色对应的页面标题和菜单 | 返回角色入口表重新选择；菜单缺失时请运营管理员检查权限 |
| 首次任务路径 | 快速入门或端到端页面显示下一步功能页链接 | 重新打开任务路径，并使用目标角色的第一个链接 |
| 调用排查入口 | **"调用日志"** 或 **"我的调用"** 显示筛选项、记录或空状态提示 | 检查时间范围、模型和日志权限 |
| 收益入口 | 模型收益、用量明细或客户调用页面显示对应标题和筛选控件 | 检查角色、账号范围和时间范围 |

## 常见问题

#### 模型发布后不可见

**问题现象：**

模型提交后，在 **"模型"** 或 **"我的模型"** 列表中找不到。

**可能原因：**

- 模型仍在审核中或发布任务未完成。
- 可见范围、供应方或标签配置不正确。
- 模型来源、模板或元模型配置不完整。

**处理方式：**

1. 查看我的模型中的审核记录和发布状态。
2. 核对可见范围、来源和模板配置。
3. 审核通过但仍不可见时，联系运营管理员检查发布索引。

#### 模型调用失败

**问题现象：**

体验中心或 API 调用返回认证失败、限流、超时或上游错误。

**可能原因：**

- Personal Key、API Key 或请求头无效。
- 模型来源 Endpoint 调用返回错误。
- 触发限流、Token 上限或计费额度限制。

**处理方式：**

1. 确认调用示例使用占位符替换为有效凭据。
2. 查看调用日志中的请求 ID、错误码和延迟。
3. 联系供应方或运营管理员核对来源健康、限流和额度。

#### 不知道应该从哪个页面开始

**问题现象：**

进入模型及AI服务后，不确定自己应该配置、发布还是调用模型。

**可能原因：**

当前账号的角色与要完成的任务没有对应起来。

**处理方式：**

1. 负责配置和审核时，先进入运营管理员入口。
2. 负责发布模型时，先进入 **"创作空间"** 下的 **"我的模型"**。
3. 负责使用模型时，先进入 **"模型"**，再进入 **"体验中心"**。
4. 页面入口仍不显示时，联系运营管理员确认角色权限；完成后应能看到对应菜单或页面标题。

#### 发布表单中找不到元模型、模型来源或模板

**问题现象：**

发布模型时，下拉框中没有目标元模型、模型来源或模板。

**可能原因：**

对象尚未创建、状态不允许使用、地域配置不匹配，或当前账号没有查看权限。

**处理方式：**

1. 请运营管理员分别检查 [元模型](./operator/settings/meta-models/)、[模型来源](./operator/settings/model-source/) 和 [模板](./operator/settings/model-templates/) 的列表与状态。
2. 核对发布区域、地域和来源配置是否匹配。
3. 重新进入发布页面并查看下拉框；仍无记录时联系运营管理员提供页面名称和筛选条件。

#### 体验中心有结果，是否可以直接接入生产

**问题现象：**

体验中心返回了结果，但正式 API 调用还没有完成验证。

**可能原因：**

体验中心使用的参数、凭据、限流或可见范围可能与生产接入不同。

**处理方式：**

1. 查看模型详情中的接入说明、协议和价格。
2. 用受控的测试凭据执行一次代表性调用，并在 **"调用日志"** 中记录请求 ID 和错误码。
3. 核对额度、限流和可见范围；页面和日志都显示预期结果后，再进入生产接入。

## 注意事项

- 本页用于选择模型及AI服务阅读路径，具体字段、按钮和状态以对应功能页为准。
- 发布、审核、下架、限流、计费配置和发布区域会影响模型可见性、调用方式和真实服务暴露范围，变更前应确认影响范围。
- 文档、截图和工单中不要暴露真实账号、Personal Key、API Key、Token、请求 ID、内部地址、测试参数、客户名称或完整调用内容。

## 后续操作

1. 运营管理员定期检查元模型、来源和模板的一致性。
2. 模型提供方发布后跟踪调用质量、收益和客户反馈。
3. 调用方正式接入前在体验中心和调用日志中完成验证。
