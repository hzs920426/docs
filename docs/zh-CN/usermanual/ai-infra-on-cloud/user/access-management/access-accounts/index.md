# 接入管理

## 功能概述

| 项目 | 内容 |
| --- | --- |
| 适用角色 | 模型提供方 |
| 导航路径 | AI基础设施 > On-Cloud > 接入管理 > 接入管理 |
| 页面路由 | `/infrahub/user/access/account` |
| 管理对象 | 模型提供方可使用的云账号和访问凭据 |

#### 新手理解

接入管理像模型提供方自己的云账号入口。添加账号后，平台才能在授权范围内发现该账号可使用的云资源并供快速部署选择。

#### 术语表

| 术语 | 英文 | 说明 |
| --- | --- | --- |
| 接入账号 | Access Account | 模型提供方用于访问云平台资源的账号。 |
| Access Key ID | Access Key ID | 凭据标识，文档示例使用 `<ACCESS_KEY_ID>`。 |
| Access Key Secret | Access Key Secret | 敏感凭据，文档示例使用 `<ACCESS_KEY_SECRET>`。 |

#### 推荐操作顺序

先查看现有账号和授权范围，再添加账号；添加后等待资源同步，并到快速部署确认云平台和地域可选。

#### 小白先看

| 场景 | 先做 | 不要直接做 |
| --- | --- | --- |
| 第一次进入页面 | 查看现有对象、状态和可用操作 | 直接修改未知对象 |
| 准备变更配置 | 核对上游依赖、影响范围和目标对象 | 跳过依赖和影响检查 |
| 操作完成 | 按结果校验表复核当前页和下游页面 | 只看成功提示不复核下游 |
| 页面出现异常 | 记录脱敏对象、时间和页面提示 | 反复提交或写入真实凭据 |

## 前提条件

1. 当前账号具备 `接入管理` 页面或流程所需权限。
2. 运营管理员已完成云平台、资源池和授权配置。
3. 新增账号前已通过安全渠道取得最小权限凭据并确认费用边界。

## 页面说明

页面展示接入账号列表和添加云账号弹窗。当前运营管理员账号未暴露此用户入口，本页按既有页面证据和跨角色流程标记为部分验证。

页面截图：

![接入账号列表](./images/access-accounts-list.png)

上图展示接入账号列表。重点核对目标对象、当前状态、字段和操作入口。

## 主要操作

### 查看接入账号

1. 进入接入管理页面。
2. 按云平台或账号名称定位目标记录。
3. 核对所属云平台、创建时间和可用状态。

### 添加接入账号

1. 点击 **"添加云账号"**。
2. 填写账号名称并选择云平台。
3. 在安全字段中填写 `<ACCESS_KEY_ID>` 和 `<ACCESS_KEY_SECRET>` 对应的真实值。
4. 点击 **"确定"** 后等待同步，并到快速部署确认资源可选。

![添加接入账号](./images/add-cloud-account.png)

上图展示添加接入账号。重点核对目标对象、当前状态、字段和操作入口。

## 参数速查表

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 云平台页签 | 否 | 页签 | `全部` | 按云平台筛选账号列表。 |
| 名称 | 否 | 输入框 | `demo-cloud-account` | 按账号名称搜索记录，文档仅使用脱敏示例。 |
| 搜索 | 否 | 按钮 | `搜索` | 按当前筛选条件查询账号记录。 |
| 重置 | 否 | 按钮 | `重置` | 清空筛选条件并恢复列表展示。 |
| 添加云账号 | 是 | 按钮 | `添加云账号` | 打开新增云账号弹窗。 |
| 账号名称 | 是 | 文本 | `demo-cloud-account` | 云账号在平台中的展示名称，避免包含真实客户、业务或内部环境信息。 |
| 选择云平台 | 是 | 下拉选择 | `示例云平台` | 选择账号所属云平台。 |
| Access Key ID | 是 | 文本 | `<ACCESS_KEY_ID>` | 云侧访问凭据标识，文档中仅使用占位示例。 |
| Access Key Secret | 是 | 密钥文本 | `<ACCESS_KEY_SECRET>` | 云侧访问密钥，禁止写入真实值。 |
| 编辑 | 否 | 操作入口 | `编辑` | 修改已有云账号配置，操作前需确认影响范围。 |
| 更多操作 | 否 | 操作入口 | `...` | 打开页面提供的更多操作入口。 |
| 取消 | 否 | 按钮 | `取消` | 关闭弹窗且不保存本次配置。 |
| 确定 | 是 | 按钮 | `确定` | 最终提交新增云账号配置，点击前必须完成复核。 |

## 踩坑提示

- 不要跳过上游依赖检查：运营管理员已完成云平台、资源池和授权配置。
- 配置变更前先确认影响：新增账号前已通过安全渠道取得最小权限凭据并确认费用边界。
- 页面成功提示不等于下游已同步，操作后必须按结果校验表复核。
- 示例凭据只使用 `<API_KEY>`、`<PERSONAL_KEY>`、`<ACCESS_KEY_ID>`、`<ACCESS_KEY_SECRET>`、`<BASE_URL>` 和 `<ENDPOINT_PATH>`。

## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 页面可访问 | 标题、导航和主要内容正常显示 | 检查角色权限和导航路径 |
| 管理对象可见 | 模型提供方可使用的云账号和访问凭据按预期显示 | 清除筛选并核对上游依赖 |
| 操作结果已保存 | 页面显示预期状态或新记录 | 查看页面提示、必填字段和依赖关系 |
| 下游结果一致 | 关联页面能看到本页变更 | 等待同步后刷新，并回到责任对象排查 |

## 常见问题

#### 接入管理中找不到目标对象

**问题现象：**

页面列表或选择器中没有预期对象。

**可能原因：**

- 查询条件仍在生效，目标对象被过滤。
- 上游对象未启用，或当前角色没有可见权限。

**处理方式：**

1. 清除筛选并刷新页面。
2. 回到前置对象核对：运营管理员已完成云平台、资源池和授权配置。
3. 确认当前角色和数据范围后重新定位目标对象。

#### 接入管理操作入口不可用

**问题现象：**

预期按钮、菜单或状态开关不可用。

**可能原因：**

- 当前账号缺少对应操作权限。
- 目标对象状态、引用关系或前置条件不允许执行该操作。

**处理方式：**

1. 核对按钮对应的权限和目标对象当前状态。
2. 检查页面提示的引用关系和前置条件。
3. 解除阻断条件后刷新页面，再执行一次。

#### 接入管理修改后下游未生效

**问题现象：**

页面提示成功，但后续页面仍显示旧状态。

**可能原因：**

- 关联页面存在缓存或同步延迟。
- 当前页与下游页使用了不同角色、租户或数据范围。

**处理方式：**

1. 等待同步完成并刷新当前页和下游页。
2. 确认两页使用相同角色、租户和对象范围。
3. 仍不一致时回到责任对象核对保存结果。

#### 接入管理数据与其他页面不一致

**问题现象：**

当前页面的数量或状态与关联页面不同。

**可能原因：**

- 两个页面的筛选条件、统计口径或更新时间不同。
- 对象变更仍在同步，或当前角色的数据权限不同。

**处理方式：**

1. 统一两页的筛选条件和统计口径。
2. 核对各页面更新时间并等待同步。
3. 按对象详情逐项比较，不只对比汇总数量。

#### 接入管理操作失败如何排查

**问题现象：**

提交后页面提示失败或状态长时间不变化。

**可能原因：**

- 必填字段、字段组合或对象状态不满足提交条件。
- 上游依赖失效、网络请求失败，或同一操作已在处理中。

**处理方式：**

1. 记录脱敏后的对象、时间和完整页面提示。
2. 核对必填字段、对象状态和上游依赖。
3. 确认没有进行中的相同任务后再重试一次。

## 注意事项

- 新增账号前已通过安全渠道取得最小权限凭据并确认费用边界。
- 文档、截图、工单和聊天记录中不得写入真实账号、凭据、内部地址或客户数据。
- 涉及授权、部署、删除、发布、启停或计费的变更，应保留可审计记录并准备恢复方案。

## 后续操作

1. 回到 `快速部署` 页面确认对应云平台和地域资源是否可见。
2. 进入 `我的部署` 查看后续部署任务是否能使用该账号。
3. 定期检查云账号权限、凭据有效期和资源可见性。
