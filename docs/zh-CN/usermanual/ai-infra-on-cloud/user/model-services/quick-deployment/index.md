# 快速部署

## 功能概述

| 项目 | 内容 |
| --- | --- |
| 适用角色 | 模型提供方 |
| 导航路径 | AI基础设施 > On-Cloud > 模型服务 > 快速部署 |
| 页面路由 | `/infrahub/user/model/store` |
| 管理对象 | 可部署模型、智能推荐、部署地域、资源规格和费用 |

#### 新手理解

快速部署像模型服务的下单向导。先选可部署模型，再让页面推荐地域和规格，确认资源、费用和授权后创建部署。

#### 术语表

| 术语 | 英文 | 说明 |
| --- | --- | --- |
| 可部署范围 | Deployable Scope | 当前账号在授权和资源条件下可选择的模型与地域。 |
| 智能推荐 | Smart Recommendation | 根据模型和资源条件推荐地域与规格。 |
| 部署费用 | Deployment Cost | 所选资源规格和运行方式对应的预计费用。 |

#### 推荐操作顺序

先确认可部署范围，再查看智能推荐和费用，最后创建部署；提交后立即到我的部署查看状态和事件。

#### 小白先看

| 场景 | 先做 | 不要直接做 |
| --- | --- | --- |
| 第一次进入页面 | 查看现有对象、状态和可用操作 | 直接修改未知对象 |
| 准备变更配置 | 核对上游依赖、影响范围和目标对象 | 跳过依赖和影响检查 |
| 操作完成 | 按结果校验表复核当前页和下游页面 | 只看成功提示不复核下游 |
| 页面出现异常 | 记录脱敏对象、时间和页面提示 | 反复提交或写入真实凭据 |

## 前提条件

1. 当前账号具备 `快速部署` 页面或流程所需权限。
2. 运营管理员已完成接入、授权、模型、框架、镜像和策略配置。
3. 部署前已确认目标地域、资源规格、预计费用、额度和停止方案。

## 页面说明

页面提供模型选择、部署地域与规格推荐，以及部署提交入口。当前运营管理员账号未暴露此用户入口，本页按既有截图和跨角色流程标记为部分验证。

页面截图：

![可部署范围](./images/deployable-scope.png)

上图展示可部署范围。重点核对目标对象、当前状态、字段和操作入口。

## 主要操作

### 查看可部署模型

1. 进入快速部署页面。
2. 按模型名称或能力定位目标模型。
3. 核对可部署地域、账号和授权范围。

### 查看智能推荐

1. 选择目标模型并打开智能推荐。
2. 比较推荐地域、资源规格、可用状态和预计费用。
3. 推荐为空时回到账号、授权、模型或策略页排查前置条件。

![智能推荐部署](./images/smart-deployment-recommendation.png)

上图展示智能推荐部署。重点核对目标对象、当前状态、字段和操作入口。

### 创建模型部署

1. 确认模型、地域、账号、规格和费用。
2. 填写部署名称和页面要求的配置。
3. 点击 **"部署"** 创建任务。
4. 进入我的部署查看状态和事件，失败时不要重复提交相同任务。

![创建模型部署](./images/publish-deployment.png)

上图展示创建模型部署。重点核对目标对象、当前状态、字段和操作入口。

## 参数速查表

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 当前范围 | 否 | 状态标签 | `示例云 / 示例地域` | 展示当前选中的可部署云平台和地域。 |
| 全部云 | 否 | 下拉筛选 | `全部云` | 按云平台筛选可部署范围。 |
| 全部地域 | 否 | 下拉筛选 | `全部地域` | 按地域筛选可部署范围。 |
| 全模型 | 否 | 下拉筛选 | `全模型` | 按模型类型筛选匹配模型。 |
| 全系列 | 否 | 下拉筛选 | `全系列` | 按模型系列筛选匹配模型。 |
| 全场景 | 否 | 下拉筛选 | `全场景` | 按模型场景筛选匹配模型。 |
| 搜索 | 否 | 输入框/按钮 | `搜索` | 按模型名称、作者、系列或来源搜索。 |
| 默认排序 | 否 | 按钮 | `默认排序` | 按页面默认规则排序模型列表。 |
| 详情 | 否 | 操作入口 | `详情` | 查看模型详情。 |
| 部署模型 | 是 | 操作入口 | `部署模型` | 进入智能推荐部署流程。 |
| 已选模型 | 是 | 展示字段 | `示例模型` | 展示当前准备部署的模型及标签信息。 |
| 部署模式 | 是 | 分段选择 | `单节点` | 选择部署模式，截图中可见单节点、验证低并发成本和高可用选项。 |
| 推理引擎 | 是 | 单选卡片 | `vLLM` | 选择部署模型使用的推理引擎。 |
| 业务策略 | 是 | 单选卡片 | `经济适用` | 选择推荐部署方案的排序偏好。 |
| 模型算力匹配方案 | 是 | 推荐卡片 | `示例云 / 示例地域` | 展示平台推荐的云平台、地域、规格和费用组合。 |
| GPU | 否 | 展示字段 | `示例 GPU x 1` | 推荐方案中的 GPU 资源。 |
| CPU / 内存 | 否 | 展示字段 | `示例 CPU / 示例内存` | 推荐方案中的 CPU 和内存规格。 |
| 费用估算 | 否 | 展示字段 | `示例费用` | 展示小时、日、月、年费用估算，文档不记录真实金额明细。 |
| 确认部署 | 否 | 按钮 | `确认部署` | 进入发布部署确认弹窗。 |
| 云账号 | 是 | 下拉选择 | `example-cloud-account` | 选择用于发起部署的云账号，文档仅使用占位示例。 |
| 任务名称 | 是 | 文本 | `demo_deploy_task` | 部署任务名称，避免写入真实业务或客户信息。 |
| 任务描述 | 否 | 多行文本 | `示例任务描述` | 补充部署说明，避免写入账号、密钥、内网地址或内部参数。 |
| 返回推荐 | 否 | 按钮 | `返回推荐` | 返回推荐方案页面，不提交部署任务。 |
| 发起部署 | 是 | 按钮 | `发起部署` | 最终发起部署任务，点击前需完成复核。 |

## 踩坑提示

- 不要跳过上游依赖检查：运营管理员已完成接入、授权、模型、框架、镜像和策略配置。
- 配置变更前先确认影响：部署前已确认目标地域、资源规格、预计费用、额度和停止方案。
- 页面成功提示不等于下游已同步，操作后必须按结果校验表复核。
- 示例凭据只使用 `<API_KEY>`、`<PERSONAL_KEY>`、`<ACCESS_KEY_ID>`、`<ACCESS_KEY_SECRET>`、`<BASE_URL>` 和 `<ENDPOINT_PATH>`。

## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 页面可访问 | 标题、导航和主要内容正常显示 | 检查角色权限和导航路径 |
| 管理对象可见 | 可部署模型、智能推荐、部署地域、资源规格和费用按预期显示 | 清除筛选并核对上游依赖 |
| 操作结果已保存 | 页面显示预期状态或新记录 | 查看页面提示、必填字段和依赖关系 |
| 下游结果一致 | 关联页面能看到本页变更 | 等待同步后刷新，并回到责任对象排查 |

## 常见问题

#### 快速部署中找不到目标对象

**问题现象：**

页面列表或选择器中没有预期对象。

**可能原因：**

- 查询条件仍在生效，目标对象被过滤。
- 上游对象未启用，或当前角色没有可见权限。

**处理方式：**

1. 清除筛选并刷新页面。
2. 回到前置对象核对：运营管理员已完成接入、授权、模型、框架、镜像和策略配置。
3. 确认当前角色和数据范围后重新定位目标对象。

#### 快速部署操作入口不可用

**问题现象：**

预期按钮、菜单或状态开关不可用。

**可能原因：**

- 当前账号缺少对应操作权限。
- 目标对象状态、引用关系或前置条件不允许执行该操作。

**处理方式：**

1. 核对按钮对应的权限和目标对象当前状态。
2. 检查页面提示的引用关系和前置条件。
3. 解除阻断条件后刷新页面，再执行一次。

#### 快速部署修改后下游未生效

**问题现象：**

页面提示成功，但后续页面仍显示旧状态。

**可能原因：**

- 关联页面存在缓存或同步延迟。
- 当前页与下游页使用了不同角色、租户或数据范围。

**处理方式：**

1. 等待同步完成并刷新当前页和下游页。
2. 确认两页使用相同角色、租户和对象范围。
3. 仍不一致时回到责任对象核对保存结果。

#### 快速部署数据与其他页面不一致

**问题现象：**

当前页面的数量或状态与关联页面不同。

**可能原因：**

- 两个页面的筛选条件、统计口径或更新时间不同。
- 对象变更仍在同步，或当前角色的数据权限不同。

**处理方式：**

1. 统一两页的筛选条件和统计口径。
2. 核对各页面更新时间并等待同步。
3. 按对象详情逐项比较，不只对比汇总数量。

#### 快速部署操作失败如何排查

**问题现象：**

提交后页面提示失败或状态长时间不变化。

**可能原因：**

- 必填字段、字段组合或对象状态不满足提交条件。
- 上游依赖失效、网络请求失败，或同一操作已在处理中。

**处理方式：**

1. 记录脱敏后的对象、时间和完整页面提示。
2. 核对必填字段、对象状态和上游依赖。
3. 确认没有进行中的相同任务后再重试一次。

## 注意事项

- 部署前已确认目标地域、资源规格、预计费用、额度和停止方案。
- 文档、截图、工单和聊天记录中不得写入真实账号、凭据、内部地址或客户数据。
- 涉及授权、部署、删除、发布、启停或计费的变更，应保留可审计记录并准备恢复方案。

## 后续操作

1. 进入 `我的部署` 查看部署状态、事件和监控。
2. 部署成功后获取脱敏调用信息，并按权限进行测试调用。
3. 持续关注费用估算、资源占用、服务状态和业务访问结果。
