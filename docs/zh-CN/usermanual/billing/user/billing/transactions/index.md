# 流水明细

::: info 文档信息
版本：v1.0
更新日期：2026-08-27
:::

## 功能概述

| 项目 | 内容 |
| --- | --- |
| 适用角色 | 模型调用方 |
| 导航路径 | 账务 > 用户账务 > 流水明细 |
| 页面路由 | `/billing/my/account/transactions/records` |
| 管理对象 | 收入流水、支出流水、交易上下文、交易单号 |

`流水明细` 页面标题显示为 `收支明细`，用于查询当前账号的账务收支记录。用户可按交易类型、收支类型和时间范围筛选，查看每一笔流水的发生时间、交易类型、上下文、额度变化、变更后额度、交易单号、备注和详情入口。


#### 新手理解

流水明细像账户的小票列表：余额为什么增加、为什么减少，都应能在这里找到对应记录。排查余额问题时，不要只看账户概览，应进入流水明细按时间、收支方向和交易类型核对来源。

#### 术语速查

| 术语 | 含义 | 处理建议 |
| --- | --- | --- |
| 收支类型 | 表示本次流水是收入还是支出。 | 先看方向，再看交易类型。 |
| 交易类型 | 转账、消费、退款、调整等业务分类。 | 用于缩小排查范围。 |
| 变更额度 | 本次流水导致的 Credits 增减。 | 与变更后额度一起看。 |
| 交易单号 | 关联业务交易的单号。 | 不等同于支付流水号。 |
| 变更后额度 | 本次流水发生后的账户额度。 | 用于解释余额变化。 |

## 前提条件

1. 当前账号具备用户侧账务查看权限。
2. 已进入 `我的账务 > 流水明细`。
3. 已确认需要查询的交易时间范围。
4. 对外沟通时应使用脱敏后的流水号或交易单号。

## 页面说明

页面包含筛选区和流水列表。筛选区支持 `交易类型`、`收支类型` 和时间范围筛选，并提供 `搜索`、`重置` 等按钮。表格展示 `流水号`、`发生时间`、`收支类型`、`交易类型`、`上下文`、`变更额度`、`变更后额度`、`交易单号`、`备注` 和 `操作`。

下图展示流水明细列表，截图中的流水号、时间、上下文和金额必须脱敏处理。

![流水明细](./images/transactions-list.png)

| 区域 | 说明 |
| --- | --- |
| 交易类型 | 按业务交易类型筛选流水。 |
| 收支类型 | 按收入或支出方向筛选流水。 |
| 时间范围 | 按流水发生时间筛选。 |
| 搜索 | 按当前筛选条件查询流水列表。 |
| 重置 | 清空筛选条件并恢复默认列表。 |
| 流水列表 | 展示流水号、发生时间、收支类型、交易类型、上下文、变更额度、变更后额度、交易单号、备注和操作。 |
| 详情 | 查看单笔流水的更多信息。 |

## 主要操作

### 核对余额变化

1. 选择一笔流水，记录变化前额度、变动方向、变动金额和变化后额度。
2. 对照相邻流水确认余额是否连续，检查是否存在充值、退款或调整。
3. 连续时前一笔变化后额度应等于后一笔变化前额度；不一致时检查时间排序和延迟入账。
4. 无法解释时记录脱敏流水号和时间范围后升级，不直接充值或调账。

![流水明细 - 核对余额变化](./images/transactions-list.png)

上图展示该操作对应的页面入口或当前状态，重点核对页面标题、目标记录和可见操作。

### 查询流水明细

1. 进入 `账务 > 用户账务 > 流水明细`。
2. 在筛选区选择 `交易类型`。
3. 选择 `收支类型`。
4. 按需要选择时间范围。
5. 点击 **"搜索"** 查看结果。
6. 如需重新查询，点击 **"重置"** 清空条件。

![流水明细 - 查询流水明细](./images/transactions-list.png)

上图展示该操作对应的页面入口或当前状态，重点核对页面标题、目标记录和可见操作。

### 查看单笔流水

1. 进入 `账务 > 用户账务 > 流水明细`。
2. 在列表中定位目标流水。
3. 核对流水号、发生时间、收支类型、交易类型和上下文。
4. 查看变更额度、变更后额度、交易单号和备注。
5. 如存在行内 `详情` 入口，点击后查看更完整信息。
6. 对外沟通时隐藏真实流水号、交易单号、账号、金额和业务上下文。

![流水明细 - 查看单笔流水](./images/transactions-list.png)

上图展示该操作对应的页面入口或当前状态，重点核对页面标题、目标记录和可见操作。

## 参数速查表

| 字段名称 | 是否必填 | 字段类型 | 示例 | 说明 |
| --- | --- | --- | --- | --- |
| 交易类型 | 否 | 枚举 | 转账 | 用于按业务交易类型筛选流水。 |
| 收支类型 | 否 | 枚举 | 收入 | 用于筛选收入或支出记录。 |
| 时间范围 | 否 | 日期范围 | 脱敏时间范围 | 用于限定流水发生时间。 |
| 搜索 | 否 | 按钮 | 搜索 | 按筛选条件刷新流水列表。 |
| 重置 | 否 | 按钮 | 重置 | 清空筛选条件。 |
| 流水号 | 系统生成 | 文本 | 脱敏流水号 | 单笔流水的唯一标识。 |
| 发生时间 | 系统生成 | 时间 | 脱敏时间 | 展示流水发生时间。 |
| 收支类型 | 系统生成 | 表格列 | 收入 | 展示流水收入或支出方向。 |
| 交易类型 | 系统生成 | 表格列 | 消费 | 展示流水业务交易类型。 |
| 上下文 | 系统生成 | 文本 | 脱敏上下文 | 展示交易关联的业务上下文。 |
| 变更额度 | 系统生成 | Credits | 脱敏金额 | 本次交易导致的额度变化。 |
| 变更后额度 | 系统生成 | Credits | 脱敏金额 | 本次交易后的账户额度。 |
| 交易单号 | 系统生成 | 文本 | 脱敏交易单号 | 与业务交易关联的单号。 |
| 备注 | 系统生成 | 文本 | 脱敏备注 | 展示流水补充说明。 |
| 详情 | 否 | 入口 | 详情 | 查看单笔流水更多信息。 |

## 踩坑提示

- 交易单号不等于支付流水号，也不一定等同于充值订单号。
- 单条流水只能解释一次额度变化，月度对账仍需进入月度账单核对汇总。
- 收入、支出、退款、冲正等方向容易混淆，先看交易类型再判断金额变化。
- 查询不到流水时先放宽时间范围，不要直接判断为数据丢失。
- 不记录真实账号、邮箱、订单号、流水号、金额、客户名、租户名、Token 或 Key。
- 截图、导出、工单和评论必须脱敏。

## 结果校验

| 检查项 | 成功表现 | 异常时处理 |
| --- | --- | --- |
| 页面加载 | 筛选区和流水列表正常显示。 | 刷新页面，或检查用户侧账务权限。 |
| 筛选可用 | 交易类型、收支类型和时间范围可用于定位流水。 | 点击 **"重置"** 后重新筛选。 |
| 列表字段可见 | 流水号、发生时间、收支类型、交易类型、金额和交易单号可见。 | 放宽时间范围或清空筛选。 |
| 详情可查看 | 行内 `详情` 可打开单笔流水信息。 | 检查权限或记录脱敏流水号咨询运营方。 |
| 流水导出受控 | 流水仅由具备权限的人员按确认范围导出。 | 如发生非预期导出，立即记录时间和范围并通知负责人复核。 |

## 常见问题

#### 找不到目标流水

**问题现象：**

按交易类型、收支类型或时间范围查询后没有目标记录。

**可能原因：**

时间范围过窄、筛选条件不匹配，或相关交易尚未完成入账。

**处理方式：**

先清空筛选条件，再扩大时间范围；如仍找不到，使用脱敏后的订单号或业务记录联系运营方核对。

#### 流水方向与预期不同

**问题现象：**

某条记录显示为收入或支出，但与预期方向不一致。

**可能原因：**

交易类型、退款、冲正、转账或调整会影响收支方向。

**处理方式：**

查看交易类型和上下文；再结合交易单号、月度账单和账户概览判断口径。

#### 流水汇总和月度账单不一致

**问题现象：**

流水明细加总结果与月度账单汇总不同。

**可能原因：**

两边账期、时间范围、交易类型或统计口径不同。

**处理方式：**

统一账期和时间范围；刷新月度账单；必要时按交易类型分组核对。


#### 流水明细状态刷新后仍未更新怎么办？

**问题现象：**

完成相关处理后，流水明细中的金额、数量或状态仍保持不变。

**可能原因：**

- 当前账期、租户、客户或业务范围与处理对象不一致。
- 上游统计、入账或结算任务仍在处理中。
- 当前账号只能看到部分数据范围。

**处理方式：**

1. 重新核对流水明细中的账期和对象范围。
2. 刷新页面并重新进入目标记录，确认更新时间。
3. 在账户概览和月度账单中交叉核对上游状态。
4. 仍未更新时，提交脱敏后的账期、对象标识、状态和更新时间给授权人员排查。

#### 共享流水明细信息前需要注意什么？

**问题现象：**

需要通过截图、工单或报告共享流水明细中的核对结果。

**可能原因：**

- 流水号、金额、交易类型、账号和关联单据可能属于敏感账务信息。
- 共享范围可能超过接收方的业务权限。
- 完整截图可能同时包含账号、环境或其他无关信息。

**处理方式：**

1. 只保留排查必需的字段、状态和时间范围。
2. 使用规定的浅灰不透明小颗粒马赛克精准覆盖敏感文字和数值。
3. 确认截图未包含顶部账号、环境信息、真实凭证或内部地址。
4. 按最小接收范围发送，并在记录中说明脱敏范围。
## 注意事项

- 流水号、交易单号、上下文和金额属于敏感账务信息，对外提供前应脱敏。
- 不要仅凭单条流水判断整月费用，应结合月度账单核对。
- 发现异常时，先记录筛选条件和脱敏后的流水标识，再联系运营方处理。
- 导出流水前，确认筛选条件、数据范围和接收方权限，并对流水号、账号和金额脱敏。

## 后续操作

1. 核对充值流水，进入 [充值订单](../top-up-orders/)。
2. 核对月度汇总，进入 [月度账单](../monthly-bill/)。
3. 查看余额概况，进入 [账户概览](../overview/)。
